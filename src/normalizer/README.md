# normalizer

Scaffold placeholder for canonical note normalization.

This directory will eventually contain the logic that converts raw source payloads into the fixed note schema documented in `docs/architecture.md`. The initial scaffold intentionally contains no runtime implementation.
