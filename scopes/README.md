# scopes

Historical scope definitions from the removed in-process QA layer.

These files are no longer consumed by `oki`. Keep them only as reference material if you want to design higher-level retrieval groupings in the repo-local Obsidian QA plugin or in external agent workflows.
