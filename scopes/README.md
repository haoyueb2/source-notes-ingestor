# scopes

Historical scope definitions from the removed in-process Q&A layer.

These files are no longer consumed by `sni`. Keep them only as reference material if you want to design higher-level retrieval groupings in external agent workflows.
