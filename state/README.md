# state

Scaffold placeholder for local sync state.

This directory is reserved for runtime checkpoints, deduplication metadata, and retry bookkeeping. The initial scaffold intentionally contains no runtime state files.
