# tests

Ingestion-focused test suite.

This directory covers adapter behavior, normalization, verification, library writing, and CLI surface checks for the ingestion runtime. Search-oriented Q&A now lives in the repo-local plugin/skill and is not tested through `sni`.
